# Copy this file to .env.local and fill it in. .env.local is never committed.

# Server-only. These never reach the browser.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
STAFF_PIN=

# Safe to expose. Only used to decide which notice the form shows.
NEXT_PUBLIC_RSVP_DEADLINE=2026-09-04
